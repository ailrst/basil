{
  relies = (z.mem == z.mem);
  guarantees = z.mem >= (z.mem);
  ensures = false;
  procedures = {
    main = {
      relies = true;
      requires = (x.gamma == true) && (z.gamma == true) && (secret.gamma == relies) && (z.mem == 0bv32) ;
      g = (forall x: bv64 :: x > 0bv64)
    }
  }
}
